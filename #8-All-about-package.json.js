// what is package.json
// => when we create any project then package.json file stores all the details & dependencies and important files used in the particular project.

// 

var colors = require('colors')
console.log('this is rameshwar namdev sutar demo'.bgGreen);

